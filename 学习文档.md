# LangChain + LangGraph 学习文档

## 1. 当前目标

先实现一个最基础的 Agent 循环：

1. 用户在命令行输入问题。
2. 程序把用户消息交给 LangGraph。
3. LangGraph 执行 Agent 节点。
4. Agent 节点调用 LLM。
5. LLM 返回回答。
6. 程序保存对话历史，进入下一轮。

当前文件是 `agent_loop.py`。

## 2. 初始化模型

```python
llm = ChatOpenAI(
    model=MODEL,
    api_key=API_KEY,
    base_url=BASE_URL,
    temperature=0.7,
    streaming=True,
)
```



- `model`：使用的模型名称。
- `api_key`：模型服务的 API Key。
- `base_url`：模型服务的接口地址。
- `temperature`：控制回答随机性，越高越发散。
- `streaming=True`：开启流式输出，让模型边生成边返回内容。

## 3. MessagesState 是什么

```python
from langgraph.graph import MessagesState
```

`MessagesState` 是 LangGraph 提供的一个常用状态类型。

它的核心字段是：

```python
state["messages"]
```

这里保存的是当前对话历史，例如：

```python
[
    HumanMessage(content="你好"),
    AIMessage(content="你好，有什么可以帮你？")
]
```

Agent 每次运行时，都会从 `state["messages"]` 里读取历史消息。

## 4. Agent 节点

```python
def agent_node(state: MessagesState) -> dict:
    response = llm.invoke([
        SystemMessage(content="You are a helpful assistant."),
        *state["messages"],
    ])
    return {"messages": [response]}
```

这个函数就是图里的一个节点。

它做了三件事：

1. 从 `state` 中读取历史消息。
2. 调用大模型生成回答。
3. 把模型回答返回给 LangGraph。

## 5. `*state["messages"]` 的含义

`*state["messages"]` 是 Python 的列表解包语法。

例如：

```python
state["messages"] = [
    HumanMessage(content="帮我写一个函数"),
    AIMessage(content="可以")
]
```

那么：

```python
[
    SystemMessage(content="You are a helpful assistant."),
    *state["messages"],
]
```

等价于：

```python
[
    SystemMessage(content="You are a helpful assistant."),
    HumanMessage(content="帮我写一个函数"),
    AIMessage(content="可以"),
]
```

作用是：把系统提示词放在最前面，然后接上完整对话历史。

## 6. 构建 LangGraph

```python
builder = StateGraph(MessagesState)
builder.add_node("agent", agent_node)
builder.add_edge(START, "agent")
builder.add_edge("agent", END)

app = builder.compile()
```

含义：

- `StateGraph(MessagesState)`：创建一个以消息为状态的图。
- `add_node("agent", agent_node)`：添加一个名为 `agent` 的节点。
- `add_edge(START, "agent")`：程序开始后进入 `agent` 节点。
- `add_edge("agent", END)`：`agent` 节点运行完后结束本轮图执行。
- `compile()`：把图编译成可运行对象。

最基础的图结构是：

```text
START -> agent -> END
```

## 7. 命令行 Agent 循环

```python
messages = []

while True:
    user_input = input("\nYou: ").strip()

    inputs = {
        "messages": [*messages, HumanMessage(content=user_input)]
    }

    ...
```

这里的 `messages` 是外部保存的对话历史。

每次用户输入后，程序都会构造新的输入：

```python
[*messages, HumanMessage(content=user_input)]
```

含义是：

1. 保留之前的全部历史消息。
2. 把本轮用户输入追加到最后。
3. 交给 LangGraph 执行。

## 8. 流式输出

当前代码使用：

```python
for stream_mode, chunk in app.stream(inputs, stream_mode=["messages", "values"]):
    ...
```

这里不是一次性等待完整回答，而是让 LangGraph 在执行过程中不断返回事件。

### 8.1 `stream_mode="messages"`

```python
if stream_mode == "messages":
    message_chunk, _metadata = chunk
    if message_chunk.content:
        print(message_chunk.content, end="", flush=True)
```

`messages` 模式用于接收模型生成过程中的内容片段。

比如模型最终要输出：

```text
你好，我可以帮你写代码。
```

流式模式下可能分成多段返回：

```text
你
好
，
我可以
帮你
写代码
。
```

程序每收到一段，就立刻打印一段。

### 8.2 `stream_mode="values"`

```python
elif stream_mode == "values":
    final_state = chunk
```

`values` 模式用于接收 LangGraph 的状态更新。

本轮结束后：

```python
messages = final_state["messages"]
```

这样可以把完整对话历史保存下来，下一轮继续使用。

## 9. `print(..., end="", flush=True)` 的作用

```python
print(message_chunk.content, end="", flush=True)
```

含义：

- `end=""`：打印后不自动换行，让每个 chunk 连起来显示。
- `flush=True`：马上把内容显示到终端，不等待缓冲区。

如果没有 `flush=True`，有些终端可能会等一会儿才显示内容，看起来就不像真正的流式输出。

## 10. 条件边 add_conditional_edges

后续如果要做多 Agent 路由，可以使用：

```python
builder.add_conditional_edges(
    "router",
    classify_intent,
    {
        "weather_agent": "weather_agent",
        "code_agent": "code_agent",
        "general_agent": "general_agent",
        "farewell": "farewell",
    }
)
```

含义是：

1. 流程运行到 `router` 节点后。
2. 调用 `classify_intent(state)` 判断用户意图。
3. 根据返回值选择下一个节点。

例如：

```python
def classify_intent(state):
    return "code_agent"
```

LangGraph 会根据映射：

```python
"code_agent": "code_agent"
```

跳转到 `code_agent` 节点。

可以理解成：

```python
result = classify_intent(state)

if result == "weather_agent":
    goto("weather_agent")
elif result == "code_agent":
    goto("code_agent")
elif result == "general_agent":
    goto("general_agent")
elif result == "farewell":
    goto("farewell")
```

## 11. 当前整体执行流程

```text
用户输入
  ↓
构造 messages
  ↓
app.stream(...)
  ↓
LangGraph 执行 agent_node
  ↓
agent_node 调用 llm
  ↓
LLM 流式生成内容
  ↓
程序一段段 print 到终端
  ↓
图执行完成
  ↓
保存 final_state["messages"]
  ↓
进入下一轮输入
```

## 12. 运行方式

建议使用当前 conda 环境运行：

```powershell
D:\app\anaconda\envs\agent_learn\python.exe agent_loop.py
```

退出命令：

```text
exit
```

或：

```text
quit
```

## 13. 简单工具调用的实现原理

当前 `agent_loop.py` 中已经加入了一个最小工具调用示例：

```python
@tool
def get_weather(city: str) -> str:
    """Get the current weather for a city."""
    weather_data = {
        "北京": "晴，18°C，北风 2 级",
        "上海": "多云，22°C，东南风 3 级",
        "深圳": "小雨，26°C，湿度较高",
        "香港": "阴，25°C，偶有阵雨",
    }
    return weather_data.get(city, f"暂时没有 {city} 的天气数据。")
```

这个工具的作用是：输入一个城市名，返回这个城市的天气信息。

### 13.1 `@tool` 的作用

```python
from langchain_core.tools import tool

@tool
def get_weather(city: str) -> str:
    """Get the current weather for a city."""
    ...
```

`@tool` 会把一个普通 Python 函数包装成 LangChain 可以识别的工具。

普通函数只是一段本地代码，而工具会额外带上这些信息：

- 工具名称：`get_weather`
- 工具描述：来自函数的 docstring
- 参数结构：这里是 `city: str`
- 返回结果：这里是字符串

模型并不会直接执行 Python 函数。模型看到的是工具的“说明书”，然后决定是否需要调用这个工具。

### 13.2 `bind_tools` 的作用

```python
tools = [get_weather]
llm_with_tools = llm.bind_tools(tools)
```

`bind_tools` 的意思是：把工具列表告诉模型。

绑定之后，模型在生成回答时就可以选择：

1. 直接回答用户。
2. 生成一个工具调用请求。

例如用户问：

```text
北京天气怎么样？
```

模型可能不会直接回答，而是先生成类似这样的工具调用意图：

```python
{
    "name": "get_weather",
    "args": {
        "city": "北京"
    }
}
```

注意：这一步只是“模型请求调用工具”，还没有真正执行工具函数。

### 13.3 Agent 节点做什么

```python
def agent_node(state: MessagesState) -> dict:
    response = llm_with_tools.invoke([
        SystemMessage(
            content=(
                "You are a helpful assistant. "
                "If the user asks about weather, call get_weather."
            )
        ),
        *state["messages"],
    ])
    return {"messages": [response]}
```

Agent 节点负责调用模型。

如果模型认为不需要工具，`response` 就是普通回答。

如果模型认为需要工具，`response` 会是一个带有 `tool_calls` 的 `AIMessage`。

可以理解成：

```text
用户问题
  ↓
agent_node 调用模型
  ↓
模型判断是否需要工具
  ↓
不需要工具：直接返回最终回答
需要工具：返回 tool_calls
```

### 13.4 `tools_condition` 的作用

```python
builder.add_conditional_edges(
    "agent",
    tools_condition,
    {
        "tools": "tools",
        "__end__": END,
    },
)
```

`tools_condition` 是 LangGraph 提供的条件判断函数。

它会检查最后一条 AI 消息里面有没有工具调用：

- 如果有 `tool_calls`，返回 `"tools"`。
- 如果没有 `tool_calls`，返回 `"__end__"`。

所以这段代码的含义是：

```text
agent 节点执行完
  ↓
检查最后一条 AI 消息
  ↓
有工具调用：进入 tools 节点
没有工具调用：结束本轮流程
```

### 13.5 `ToolNode` 的作用

```python
builder.add_node("tools", ToolNode(tools))
```

`ToolNode` 是真正执行工具的节点。

当它看到上一条 AI 消息里有：

```python
tool_calls = [
    {
        "name": "get_weather",
        "args": {"city": "北京"}
    }
]
```

它会自动做这些事：

1. 找到名为 `get_weather` 的工具。
2. 把参数 `city="北京"` 传进去。
3. 执行 Python 函数。
4. 把返回结果包装成 `ToolMessage`。
5. 追加回 `state["messages"]`。

工具执行后，消息历史大概会变成：

```text
HumanMessage: 北京天气怎么样？
AIMessage: 我需要调用 get_weather 工具
ToolMessage: 晴，18°C，北风 2 级
```

### 13.6 为什么 tools 后面还要回到 agent

```python
builder.add_edge("tools", "agent")
```

工具只负责返回原始结果，不负责组织最终语言。

比如工具返回：

```text
晴，18°C，北风 2 级
```

这个结果还需要再次交给模型，让模型整理成自然语言：

```text
北京现在是晴天，气温 18°C，北风 2 级，整体比较舒适。
```

所以工具调用流程不是：

```text
agent -> tools -> END
```

而是：

```text
agent -> tools -> agent -> END
```

第一次进入 `agent`：模型判断要不要调用工具。  
进入 `tools`：真正执行工具。  
第二次进入 `agent`：模型读取工具结果，生成最终回答。

### 13.7 当前工具调用图结构

当前图结构是：

```text
START
  ↓
agent
  ↓
tools_condition
  ├── 有工具调用 -> tools -> agent
  └── 无工具调用 -> END
```

更完整的执行流程是：

```text
用户输入：“北京天气怎么样？”
  ↓
保存为 HumanMessage
  ↓
进入 agent 节点
  ↓
模型发现需要天气工具
  ↓
返回带 tool_calls 的 AIMessage
  ↓
tools_condition 判断有工具调用
  ↓
进入 tools 节点
  ↓
ToolNode 执行 get_weather(city="北京")
  ↓
工具结果保存为 ToolMessage
  ↓
回到 agent 节点
  ↓
模型读取 ToolMessage
  ↓
生成最终自然语言回答
  ↓
END
```

### 13.8 工具调用和普通回答的区别

普通回答：

```text
HumanMessage
  ↓
AIMessage
  ↓
END
```

工具调用：

```text
HumanMessage
  ↓
AIMessage(tool_calls)
  ↓
ToolMessage
  ↓
AIMessage(final answer)
  ↓
END
```

也就是说，工具调用至少会多出两类消息：

- 带 `tool_calls` 的 `AIMessage`
- 工具执行结果 `ToolMessage`

### 13.9 如何测试

运行：

```powershell
D:\app\anaconda\envs\agent_learn\python.exe agent_loop.py
```

然后输入：

```text
北京天气怎么样？
```

如果模型正确触发工具，大致流程是：

1. 模型决定调用 `get_weather`。
2. `ToolNode` 执行本地函数。
3. 模型根据工具结果生成最终回答。

也可以试试：

```text
帮我解释一下 Python 的列表推导式
```

这个问题不需要天气工具，模型应该直接回答，不进入 `tools` 节点。

## 14. 工具到底在哪里执行

当前代码里没有手写这样的函数：

```python
def tool_node(state):
    ...
```

是因为 LangGraph 已经提供了内置工具节点：

```python
builder.add_node("tools", ToolNode(tools))
```

这里的 `"tools"` 是节点名字。

真正负责执行工具的是：

```python
ToolNode(tools)
```

也就是说，当前图里其实有两个主要节点：

```python
builder.add_node("agent", agent_node)
builder.add_node("tools", ToolNode(tools))
```

含义是：

- `agent` 节点：调用 LLM，让模型决定要不要调用工具。
- `tools` 节点：读取模型生成的 `tool_calls`，真正执行 Python 工具函数。

### 14.1 工具函数在哪里定义

工具函数现在被拆到了 `agent_tools.py`：

```python
@tool
def get_weather(city: str) -> str:
    ...


@tool
def run_bash_command(command: str) -> str:
    ...


tools = [get_weather, run_bash_command]
```

`agent_loop.py` 里通过下面这句导入工具列表：

```python
from agent_tools import tools
```

然后把工具列表交给两个地方：

```python
llm_with_tools = llm.bind_tools(tools)
builder.add_node("tools", ToolNode(tools))
```

这两个地方职责不同。

### 14.2 `llm.bind_tools(tools)` 做什么

```python
llm_with_tools = llm.bind_tools(tools)
```

这一步是把工具说明交给模型。

模型知道现在有这些工具：

- `get_weather`
- `run_bash_command`

但模型不会直接执行 Python 函数。

它只能生成一个“工具调用请求”，例如：

```python
tool_calls = [
    {
        "name": "get_weather",
        "args": {"city": "北京"}
    }
]
```

所以 `bind_tools` 的作用是：

```text
让模型知道有哪些工具可以用
```

不是：

```text
真正执行工具
```

### 14.3 `ToolNode(tools)` 做什么

```python
builder.add_node("tools", ToolNode(tools))
```

这一步才是创建真正的工具执行节点。

当 `ToolNode` 收到上一条 `AIMessage`，发现里面有：

```python
tool_calls = [
    {
        "name": "get_weather",
        "args": {"city": "北京"}
    }
]
```

它会自动：

1. 在 `tools` 列表里找到名字叫 `get_weather` 的工具。
2. 取出参数 `{"city": "北京"}`。
3. 执行 Python 函数 `get_weather(city="北京")`。
4. 把函数返回值包装成 `ToolMessage`。
5. 把 `ToolMessage` 追加到 `state["messages"]`。

所以工具执行发生在：

```python
ToolNode(tools)
```

不是发生在：

```python
llm.bind_tools(tools)
```

### 14.4 `tools_condition` 如何决定是否进入工具节点

```python
builder.add_conditional_edges(
    "agent",
    tools_condition,
    {
        "tools": "tools",
        "__end__": END,
    },
)
```

`tools_condition` 会检查 `agent` 节点返回的最后一条消息。

如果最后一条消息是普通回答：

```python
AIMessage(content="...")
```

它会返回：

```python
"__end__"
```

流程结束。

如果最后一条消息包含工具调用：

```python
AIMessage(
    content="",
    tool_calls=[...]
)
```

它会返回：

```python
"tools"
```

流程进入工具节点：

```python
builder.add_node("tools", ToolNode(tools))
```

### 14.5 完整工具执行链路

以“北京天气怎么样？”为例：

```text
用户输入
  ↓
agent_node 调用 llm_with_tools
  ↓
模型生成 AIMessage(tool_calls=[...])
  ↓
tools_condition 发现有 tool_calls
  ↓
进入 tools 节点
  ↓
ToolNode(tools) 执行 get_weather(city="北京")
  ↓
工具结果变成 ToolMessage
  ↓
builder.add_edge("tools", "agent")
  ↓
再次回到 agent_node
  ↓
模型读取 ToolMessage
  ↓
生成最终自然语言回答
```

### 14.6 为什么工具执行后还要回到 agent

当前代码有这一行：

```python
builder.add_edge("tools", "agent")
```

这是因为工具返回的通常是原始结果，例如：

```text
晴，18°C，北风 2 级
```

它还不是最终用户友好的回答。

回到 `agent` 后，模型会把工具结果整理成自然语言：

```text
北京现在是晴天，气温 18°C，北风 2 级，整体比较舒适。
```

所以工具调用的完整结构是：

```text
agent -> tools -> agent -> END
```

而不是：

```text
agent -> tools -> END
```

### 14.7 当前文件分工

目前项目拆成了三个核心 Python 文件：

```text
agent_loop.py    负责 LLM、LangGraph、命令行循环
agent_tools.py   负责工具定义和 tools 列表
agent_debug.py   负责调试打印和消息格式化
```

这让主流程更清晰：

```python
from agent_debug import debug_print, format_message, format_messages
from agent_tools import tools
```

`agent_loop.py` 不需要关心每个工具的具体实现，只需要知道有一个工具列表：

```python
tools
```

然后交给：

```python
llm.bind_tools(tools)
ToolNode(tools)
```

## 15. 配置文件分层

现在项目把配置分成两类。

### 15.1 `.env` 放敏感或环境相关配置

`.env` 适合放不应该提交到 Git 的信息，例如：

```env
ALIYUN_API_KEY=...
ALIYUN_BASE_URL=...
```

这些信息通常和账号、服务地址、运行环境有关。

`.env` 已经被 `.gitignore` 忽略，不会进入 Git 提交。

### 15.2 `agent_config.py` 放普通运行配置

非密钥配置放在 `agent_config.py`：

```python
DEBUG_AGENT = False
MAX_GRAPH_STEPS = 9
BASH_PATH = "bash"
MODEL = "deepseek-v4-flash"
```

这些配置不是秘密，可以提交到 Git，方便记录项目默认行为。

当前使用方式：

```python
from agent_config import MAX_GRAPH_STEPS
```

```python
from agent_config import DEBUG_AGENT
```

```python
from agent_config import BASH_PATH
```

```python
from agent_config import MODEL
```

### 15.3 为什么不都放 `.env`

如果把所有配置都放 `.env`，会有两个问题：

1. `.env` 不提交到 Git，项目默认行为不容易被记录。
2. 密钥和普通开关混在一起，时间久了不清楚哪些能公开、哪些不能公开。

所以现在的约定是：

```text
.env             密钥、服务地址、本地私有环境
agent_config.py  普通运行参数、调试开关、循环限制
```

## 16. 隔离容器执行命令

直接让 Agent 执行本地 Bash 命令风险比较高。即使做了白名单限制，模型仍然可能尝试访问敏感文件或构造危险参数。

### 16.1 执行命令的安全方案总览

从安全到灵活，可以把方案分成几层：

```text
1. 只预览命令，不执行
2. 使用结构化工具，不暴露 shell
3. 本地只读白名单命令
4. 人工确认后执行
5. 隔离容器执行
6. 容器中生成 diff，人工确认后写回
```

第一种最安全，但不能自动完成任务。  
第二种适合多数 Agent 工具，比如 `list_files`、`read_file`、`get_python_version`。  
第三种仍然有风险，因为模型可以尝试构造复杂参数。  
第四种适合学习和半自动场景。  
第五种适合执行测试、查看文件、运行只读检查。  
第六种适合未来支持“写文件”，但仍然不让容器直接修改真实项目。

当前项目实现的是：

```text
隔离容器执行 + 脱敏副本 + 只读挂载
```

也就是第 5 种。

当前更安全的方案是新增容器工具：

```python
run_command_in_container(command: str)
```

这个工具定义在 `agent_tools.py`，并且已经替换掉对外暴露的本地 Bash 工具：

```python
tools = [get_weather, run_command_in_container]
```

也就是说，Agent 现在能看到的是容器命令工具，而不是本地 `run_bash_command`。

### 16.2 为什么不直接挂载项目目录

如果直接把当前项目目录挂载到容器：

```text
当前项目目录 -> /workspace
```

容器虽然不能修改宿主机文件，但仍然能读取 `.env`。

这会泄露：

```text
API Key
base_url
其他本地私有配置
```

所以当前实现不会直接挂载原始项目目录。

### 16.3 脱敏工作区

执行命令前，程序会先创建临时目录：

```python
with tempfile.TemporaryDirectory(prefix="agent_sandbox_") as sandbox_dir:
    _copy_sanitized_workspace(sandbox_dir)
```

然后把当前项目复制进去，但排除敏感文件和缓存目录：

```python
SANDBOX_EXCLUDES = {
    ".env",
    ".git",
    ".vscode",
    "__pycache__",
    ".ipynb_checkpoints",
}
```

这样容器看到的是一份“干净副本”，而不是原始工作区。

### 16.4 容器能读取什么

容器中可以读取脱敏副本里的文件内容。

例如容器内的工作目录是：

```text
/workspace
```

如果脱敏副本里有：

```text
agent_loop.py
agent_tools.py
agent_config.py
学习文档.md
```

那么容器可以执行：

```bash
ls
cat agent_loop.py
sed -n '1,80p' agent_tools.py
```

但容器读不到这些内容：

```text
.env
.git
.vscode
__pycache__
.ipynb_checkpoints
*.pyc
```

因为它们在复制阶段就被排除了。

注意：如果你把密钥直接写进普通源码文件，比如 `agent_config.py`，那它仍然会被复制进容器。敏感信息必须继续放在 `.env`。

### 16.5 Docker 安全参数

容器执行时使用了这些限制：

```python
docker run \
  --rm \
  --network none \
  --cpus 0.5 \
  --memory 256m \
  --pids-limit 128 \
  --read-only \
  --cap-drop ALL \
  --security-opt no-new-privileges \
  --user 65534:65534 \
  --workdir /workspace \
  --mount type=bind,source=<sandbox>,target=/workspace,readonly \
  --tmpfs /tmp:rw,nosuid,nodev,size=64m \
  python:3.12-slim \
  bash -lc "<command>"
```

含义：

- `--rm`：执行完自动删除容器。
- `--network none`：禁用网络。
- `--cpus`：限制 CPU。
- `--memory`：限制内存。
- `--pids-limit`：限制进程数量。
- `--read-only`：容器根文件系统只读。
- `--cap-drop ALL`：移除 Linux capabilities。
- `--security-opt no-new-privileges`：禁止提权。
- `--user 65534:65534`：使用非 root 用户。
- `readonly mount`：项目副本只读挂载。
- `--tmpfs /tmp`：只允许在临时内存目录写临时文件。

### 16.6 容器是在哪里创建的

容器创建发生在 `agent_tools.py` 的 `run_command_in_container()` 中。

先拼出 Docker 命令：

```python
docker_command = [
    "docker",
    "run",
    "--rm",
    "--network",
    "none",
    ...
    DOCKER_IMAGE,
    "bash",
    "-lc",
    command,
]
```

真正启动容器的是：

```python
result = subprocess.run(
    docker_command,
    capture_output=True,
    text=True,
    timeout=DOCKER_TIMEOUT_SECONDS,
    check=False,
)
```

所以实际执行的是：

```text
docker run ... python:3.12-slim bash -lc "<command>"
```

`docker run` 会基于 `DOCKER_IMAGE` 创建临时容器，执行命令，然后因为有 `--rm` 自动删除容器。

### 16.7 `DOCKER_IMAGE` 是什么

`DOCKER_IMAGE` 在 `agent_config.py` 中配置：

```python
DOCKER_IMAGE = "python:3.12-slim"
```

它表示容器使用的基础镜像。

第一次运行时，如果本地没有这个镜像，Docker 会自动尝试从 Docker Hub 拉取。也可以提前手动执行：

```bash
docker pull python:3.12-slim
```

如果要换镜像，可以改成：

```python
DOCKER_IMAGE = "ubuntu:24.04"
```

或你自己构建的镜像：

```python
DOCKER_IMAGE = "my-agent-sandbox:latest"
```

但当前工具会执行：

```bash
bash -lc "<command>"
```

所以镜像里需要有 `bash`。

### 16.8 配置项

容器配置放在 `agent_config.py`：

```python
DOCKER_IMAGE = "python:3.12-slim"
DOCKER_TIMEOUT_SECONDS = 10
DOCKER_MEMORY = "256m"
DOCKER_CPUS = "0.5"
DOCKER_OUTPUT_LIMIT = 4000
```

这些配置不是密钥，可以提交到 Git。

### 16.9 执行流程

完整流程是：

```text
Agent 请求执行命令
  ↓
run_command_in_container(command)
  ↓
检查 Docker 是否可用
  ↓
复制当前项目到临时脱敏目录
  ↓
排除 .env、.git、缓存目录
  ↓
启动无网络、低权限、只读容器
  ↓
在 /workspace 执行命令
  ↓
截断过长输出
  ↓
临时目录和容器销毁
```

### 16.10 当前实现了什么

当前项目已经实现：

```text
已实现：容器内执行命令
已实现：复制脱敏工作区
已实现：排除 .env、.git、本地缓存
已实现：只读挂载项目副本
已实现：禁用网络
已实现：限制 CPU、内存、进程数、超时时间
已实现：非 root 用户执行
已实现：执行后自动删除容器和临时目录
已实现：限制输出长度
```

当前项目没有实现：

```text
未实现：容器直接写回真实项目
未实现：人工确认后应用 patch
未实现：允许访问任意本地目录
未实现：联网安装依赖
未实现：挂载 Docker socket
```

这是有意为之。当前目标是让 Agent 能安全查看和检查项目，而不是直接修改真实文件系统。

### 16.11 如果未来要写文件怎么办

不推荐这样做：

```text
docker -v 当前项目:/workspace:rw
```

因为这会让容器直接修改真实项目。如果模型执行危险命令，真实文件会受影响。

更推荐的写文件方案是：

```text
1. 复制脱敏项目到临时目录
2. 容器在临时目录中执行修改
3. 生成 diff/patch
4. 把 diff 展示给用户
5. 用户确认后，由宿主机应用 patch
```

也就是说：

```text
Agent 负责提出改动
容器负责在临时副本中验证改动
用户负责确认
宿主机负责真正写入
```

这种方式比直接给容器写权限更安全。

### 16.12 当前限制

这个方案比本地 Bash 安全很多，但不是绝对安全。

仍然建议：

1. 只用来执行查看、测试、静态检查这类命令。
2. 不要把真实生产密钥复制进工作区。
3. 不要给容器开启网络，除非你非常确定需要。
4. 不要挂载 Docker socket。
5. 如果要生产使用，最好再加人工确认步骤。

当前本机如果 Docker Desktop 没有启动，工具会返回提示：

```text
Docker 不可用。请确认 Docker Desktop 已启动。
```

### 16.13 长文件要分段读取

容器工具会限制输出长度：

```python
DOCKER_OUTPUT_LIMIT = 4000
```

如果直接执行：

```bash
cat 学习文档.md
```

文件太长时，工具只会返回前面一部分，并显示输出被截断。

因此系统提示中要求 Agent 不要直接 `cat` 大文件。

为了减少工具调用次数，当前项目新增了专门的文件读取工具：

```python
read_workspace_file(path: str, start_line: int = 1, max_lines: int = FILE_READ_CHUNK_LINES)
```

它会一次返回：

```text
文件总行数
当前读取的行号范围
带行号的文件内容
下一段建议 start_line
```

例如：

```text
read_workspace_file(path="学习文档.md", start_line=1)
```

返回中会包含：

```text
学习文档.md 共 1415 行，当前读取 1-120 行。
...
下一段可调用 read_workspace_file(path='学习文档.md', start_line=121)
```

这样一次 tool call 就能完成“查看文件行数 + 读取当前片段 + 知道下一段位置”，比让模型分别执行 `wc -l` 和 `sed` 更省工具调用次数。

如果必须通过容器命令读取，再按下面流程：

```text
1. 先用 wc -l 查看文件总行数
2. 用 grep -n 查找相关章节或关键词
3. 用 sed -n 分段读取
4. 如果输出被截断，继续读取下一段，不重复同一条命令
```

例如：

```bash
wc -l 学习文档.md
```

```bash
grep -n "隔离容器执行命令" 学习文档.md
```

```bash
sed -n '1,120p' 学习文档.md
```

```bash
sed -n '121,240p' 学习文档.md
```

默认每段行数配置在 `agent_config.py`：

```python
FILE_READ_CHUNK_LINES = 120
```

`agent_loop.py` 会把这个规则写进系统提示：

```python
"Do not read large files with plain cat. First inspect file size "
"with wc -l, locate relevant sections with grep -n, then read "
f"files in chunks of at most {FILE_READ_CHUNK_LINES} lines using "
"commands like sed -n '1,120p' path."
```

当前更推荐的规则是：

```text
读取工作区文件：优先使用 read_workspace_file
执行检查命令：使用 run_command_in_container
```

### 16.14 通用子 Agent 委派

即使 `read_workspace_file` 可以分段读取，大文件或多步骤任务仍然会带来一个问题：

```text
父 Agent 每调用一次工具，都会消耗主图执行步数
```

而当前图有最大执行步数限制：

```python
MAX_GRAPH_STEPS = 11
```

因此当前项目不再使用专用 `file_agent`，而是新增通用子 Agent：

```text
agent_subagent.py
```

父 Agent 可以调用：

```python
delegate_to_subagent(task: str, context: str = "")
```

它适合处理：

```text
大文件阅读
多步骤查找
需要压缩上下文的调查任务
需要多次工具调用但只想给父 Agent 返回摘要的任务
```

#### 为什么用通用子 Agent

专用文件子 Agent 只能处理文件阅读。通用子 Agent 更灵活：

```text
父 Agent 负责判断是否需要委派
子 Agent 负责执行受限的多步骤任务
子 Agent 完成后只返回总结
父 Agent 基于总结继续回答用户
```

这样可以把大量中间上下文留在子 Agent 内部，父 Agent 只接收压缩后的结果。

#### 子 Agent 是否继承父 Agent 上下文

子 Agent 不会继承父 Agent 的完整上下文，因为那样无法节省上下文窗口。

当前实现会通过 `InjectedState` 读取父图当前状态，并把最近若干条消息压缩成上下文传给子 Agent：

```python
state: Annotated[dict, InjectedState()]
```

同时，父 Agent 也可以通过 `context` 参数显式传入必要背景：

```python
delegate_to_subagent(
    task="总结学习文档中关于容器安全执行命令的设计",
    context="用户正在学习 LangGraph 工具调用和子 Agent 委派"
)
```

推荐原则是：

```text
传必要上下文摘要，不传完整历史
```

#### 子 Agent 不能继续创建子 Agent

这个限制非常重要。当前工具分层是：

```python
base_tools = [
    get_weather,
    read_workspace_file,
    run_command_in_container,
]
```

父 Agent 使用：

```python
parent_tools = [
    get_weather,
    run_command_in_container,
    delegate_to_subagent,
]
```

子 Agent 使用：

```python
base_tools
```

也就是说：

```text
父 Agent 有 delegate_to_subagent
子 Agent 没有 delegate_to_subagent
```

这样可以避免递归委派：

```text
父 Agent -> 子 Agent -> 孙 Agent -> ...
```

同时，父 Agent 不再拥有完整的 `read_workspace_file`。它只拥有轻量版本 `read_workspace_file_lite`。这是一条结构性约束：

```text
父 Agent 可以用 read_workspace_file_lite 看很小的文件片段
父 Agent 不能连续用 read_workspace_file_lite 啃大文件
父 Agent 必须把文件总结、文件搜索、大范围阅读任务交给子 Agent
子 Agent 才拥有完整的 read_workspace_file
```

这样比单纯依赖系统提示更可靠。

#### 当前相关配置

配置在 `agent_config.py`：

```python
SUBAGENT_MAX_STEPS = 8
SUBAGENT_CONTEXT_MESSAGE_LIMIT = 8
SUBAGENT_RESULT_LIMIT = 6000
```

含义：

- `SUBAGENT_MAX_STEPS`：子 Agent 内部最多执行多少图步骤。
- `SUBAGENT_CONTEXT_MESSAGE_LIMIT`：从父 Agent 状态中继承最近多少条消息。
- `SUBAGENT_RESULT_LIMIT`：返回给父 Agent 的总结最大长度。

#### 三种能力的分工

```text
read_workspace_file_lite
  父 Agent 使用，适合看一小段配置、函数、错误附近代码

read_workspace_file
  只给子 Agent 使用，适合读取具体行段、小片段、精确引用

delegate_to_subagent
  父 Agent 使用，适合文件阅读、大文件总结、多步骤调查、压缩上下文

run_command_in_container
  适合运行检查命令、测试命令、ls/grep 等 shell 操作
```

#### 注意

子 Agent 内部会额外调用 LLM，所以它不消耗父图的工具循环次数，但仍然会消耗模型调用成本和时间。

### 16.15 大文件全文总结使用 map-reduce 工具

即使有通用子 Agent，如果让子 Agent 自己一段段决定是否继续读取，仍然可能遇到：

```text
SUBAGENT_MAX_STEPS 不够
```

因为子 Agent 的循环仍然是：

```text
思考 -> 调工具 -> 思考 -> 调工具 -> ...
```

对于“总结整篇大文件”这种任务，更稳定的方式是确定性 map-reduce：

```text
工具内部遍历文件
  ↓
按块读取
  ↓
map 阶段并行调用 LLM，为每个块提取相关信息
  ↓
合并所有块摘要
  ↓
返回最终答案
```

当前项目新增了子 Agent 可用工具：

```python
summarize_large_file(path: str, question: str)
```

它只在子 Agent 的 `base_tools` 里：

```python
base_tools = [
    get_weather,
    read_workspace_file,
    summarize_large_file,
    run_command_in_container,
]
```

父 Agent 不直接拥有这个工具。父 Agent 遇到大文件全文问题时，应该先调用：

```python
delegate_to_subagent(...)
```

然后由子 Agent 判断是否调用：

```python
summarize_large_file(...)
```

#### 相关配置

配置在 `agent_config.py`：

```python
LARGE_FILE_CHUNK_LINES = 220
LARGE_FILE_MAX_CHUNKS = 80
LARGE_FILE_MAP_WORKERS = 4
LARGE_FILE_SUMMARY_LIMIT = 16000
```

含义：

- `LARGE_FILE_CHUNK_LINES`：每个分块读取多少行。
- `LARGE_FILE_MAX_CHUNKS`：最多处理多少个分块，控制成本。
- `LARGE_FILE_MAP_WORKERS`：map 阶段最多并行处理多少个分块。
- `LARGE_FILE_SUMMARY_LIMIT`：最终摘要最大长度。

#### 它和子 Agent 循环的关系

`summarize_large_file` 是一次工具调用。

在子 Agent 视角：

```text
调用 summarize_large_file 一次
```

但工具内部可以处理很多文件分块。

所以它不会因为“每读一段都要回到子 Agent 思考”而快速耗尽 `SUBAGENT_MAX_STEPS`。

map 阶段使用线程池并行处理分块：

```python
with ThreadPoolExecutor(max_workers=LARGE_FILE_MAP_WORKERS) as executor:
    ...
```

并行结果会按原始行号排序后再进入 reduce 阶段，避免摘要顺序混乱。

#### 推荐使用规则

```text
小片段：父 Agent 用 read_workspace_file_lite
具体片段：子 Agent 用 read_workspace_file
整篇大文件：子 Agent 用 summarize_large_file
多步骤调查：父 Agent 用 delegate_to_subagent
```

### 16.16 上下文管理模块

Agent 如果一直把完整 `messages` 历史传给模型，会遇到两个问题：

```text
输入窗口有限，历史消息最终会放不下
上下文过长会稀释当前任务重点，导致模型更容易失准或幻觉
```

因此当前项目新增了独立模块：

```text
agent_context.py
```

它负责把“完整历史消息”变成“可控输入上下文”。

#### 当前实现

核心数据结构：

```python
@dataclass
class AgentContextState:
    summary: str = ""
    recent_messages: list = field(default_factory=list)
```

含义：

- `summary`：被压缩后的长期会话摘要。
- `recent_messages`：近期原始消息缓冲区。

核心类：

```python
class AgentContextManager:
    def build_input_messages(...)
    def update_after_turn(...)
```

`build_input_messages` 用于构造本轮传给 LangGraph 的输入：

```text
会话摘要 SystemMessage
+ 最近若干条原始消息
+ 当前用户消息
```

`update_after_turn` 用于在一轮图执行结束后更新上下文状态：

```text
如果消息数量还没超过阈值：
  保留短期缓冲区

如果消息数量超过阈值：
  把旧消息压缩进 summary
  只保留最近若干条原始消息
```

#### 当前相关配置

配置位于 `agent_config.py`：

```python
RECENT_MESSAGE_LIMIT = 8
SUMMARY_TRIGGER_MESSAGE_LIMIT = 16
SESSION_SUMMARY_MAX_CHARS = 4000
SUMMARY_SOURCE_CHAR_LIMIT = 12000
```

含义：

- `RECENT_MESSAGE_LIMIT`：每轮最多把多少条近期原始消息放入模型输入。
- `SUMMARY_TRIGGER_MESSAGE_LIMIT`：缓冲区超过多少条消息后触发摘要压缩。
- `SESSION_SUMMARY_MAX_CHARS`：会话摘要最大字符数。
- `SUMMARY_SOURCE_CHAR_LIMIT`：生成摘要时最多喂给摘要模型多少字符的旧消息。

#### 为什么不直接删除旧消息

直接只保留最近 N 条消息会丢失长期决策，例如：

```text
配置为什么从 .env 挪到 agent_config.py
为什么父 Agent 没有完整 read_workspace_file
为什么大文件阅读交给子 Agent
为什么 SSE 被拆到 agent_stream.py
```

上下文管理模块会先保留一个短缓冲区，超过阈值后再将旧内容压缩成摘要。

这样可以同时满足：

```text
当前任务需要的近期细节：保留原始消息
长期架构决策和用户偏好：保留摘要
大文件、skill、项目内容：继续按需用工具读取
```

#### 和 agent_loop.py 的关系

原来命令行循环直接维护：

```python
messages = final_state["messages"]
```

这会让上下文不断增长。

现在改为：

```python
context_manager = AgentContextManager()
context_state = AgentContextState()

input_messages = context_manager.build_input_messages(context_state, user_input)

for item in stream_graph_events(app, input_messages):
    ...
    if item["event"] == "done":
        context_state = context_manager.update_after_turn(
            context_state,
            item["data"]["messages"],
        )
```

也就是说：

```text
agent_loop.py      负责交互循环
agent_stream.py    负责 LangGraph 流式事件
agent_context.py   负责上下文裁剪和摘要
```

#### 设计原则

上下文窗口里只放当前任务真正需要的信息：

```text
短期原文：最近几轮消息
长期状态：结构化摘要
外部资料：通过工具按需读取
```

不要把所有历史消息、所有文件内容、所有 skill 内容长期放进上下文。

含义：
### 17. PostgreSQL 记忆系统

当前项目已经把“上下文压缩”和“完整历史落盘”拆开处理。

#### 17.1 为什么需要记忆系统

只做上下文压缩有两个问题：

1. `AgentContextState` 默认只存在内存里，程序重启后会丢失。
2. 压缩摘要适合继续对话，但不适合作为完整历史记录。

因此现在使用 PostgreSQL 保存三类数据：

```text
agent_messages   完整对话归档
agent_sessions   短期上下文状态
agent_memories   长期记忆
```

#### 17.2 数据库配置

配置在 `agent_config.py` 中：

```python
MEMORY_ENABLED = True
MEMORY_DB_HOST = "localhost"
MEMORY_DB_PORT = 5432
MEMORY_DB_NAME = "learn_agent"
MEMORY_DB_USER = "postgres"
MEMORY_DB_PASSWORD = "postgres"
DEFAULT_SESSION_ID = "default"
```

需要先安装 PostgreSQL Python 驱动：

```bash
pip install psycopg[binary]
```

还需要确保本地数据库存在：

```bash
createdb -U postgres learn_agent
```

如果使用 Docker，可以参考 `agent_config.py` 中的 `START_DB` 命令。

#### 17.3 完整历史如何保存

每一轮对话结束后，程序会把本轮消息追加写入 `agent_messages`：

```text
用户输入
AI 工具调用请求
ToolMessage 工具结果
AI 最终回答
```

这张表用于历史展示、审计、debug 和后续重新提取记忆。

注意：完整历史不会直接全部塞回模型上下文。

#### 17.4 短期记忆如何恢复

`agent_sessions` 保存当前会话的压缩状态：

```text
summary          旧消息压缩后的摘要
recent_messages  最近若干条原始消息
turn_index       当前会话轮次
```

程序重启后，会通过 `session_id` 读取这张表，恢复：

```text
会话摘要 + 最近原始消息
```

所以历史会话重新打开时：

```text
界面显示：读取 agent_messages，展示完整历史
模型上下文：读取 agent_sessions，只加载摘要和最近消息
```

#### 17.5 长期记忆存什么

`agent_memories` 只保存稳定、有复用价值的信息，例如：

```text
user_preference  用户偏好
project_fact     项目事实
decision         架构决策
task_state       长期任务状态
skill_note       可复用经验
```

不应该保存：

```text
API Key
密码
token
.env 内容
大段工具输出
临时推理过程
一次性闲聊
```

每轮结束后，`agent_memory.py` 会调用非流式 LLM 从本轮消息中提取候选长期记忆。只有重要性达到阈值、且不包含敏感关键词的内容才会写入。

#### 17.6 每轮执行流程

当前主循环的大致流程是：

```text
1. 从 agent_sessions 加载短期上下文
2. 根据用户输入从 agent_memories 检索相关长期记忆
3. 构造模型输入：
   system prompt
   + session summary
   + relevant long-term memory
   + recent messages
   + current user input
4. 执行 LangGraph agent/tool 循环
5. 将本轮完整消息写入 agent_messages
6. 更新 agent_sessions
7. 从本轮消息中提取长期记忆，写入 agent_memories
```

核心原则：

```text
完整历史用于保存和展示
短期摘要用于继续当前会话
长期记忆用于跨会话复用
模型输入永远只加载当前任务相关的小部分信息
```

#### 17.7 回复后卡顿优化

AI 回复完成后，程序还会做本轮收尾工作：

```text
1. 完整消息归档到 agent_messages
2. 更新短期上下文 agent_sessions
3. 必要时压缩上下文 summary
4. 提取长期记忆 agent_memories
```

其中长期记忆提取需要再次调用 LLM，最容易造成“回复已经结束，但迟迟不出现下一次 You:”的卡顿。

当前实现做了两点优化：

```python
MEMORY_EXTRACTION_ASYNC = True
```

长期记忆提取会放到后台线程执行，不阻塞用户继续输入。

同时，长期记忆不会每轮都提取。当前只有满足任一条件时才会触发：

```text
1. 用户输入包含“记住 / 偏好 / 约定 / 规则 / 以后”等提示词
2. 每隔 MEMORY_EXTRACTION_INTERVAL_TURNS 轮
3. 本轮消息总字符数超过 MEMORY_EXTRACTION_MIN_CHARS
```

对应配置：

```python
MEMORY_EXTRACTION_INTERVAL_TURNS = 5
MEMORY_EXTRACTION_MIN_CHARS = 1200
```

这样可以避免把长期记忆系统变成“每轮都调用一次模型”的昂贵流程。

同时，短期上下文压缩阈值也放宽：

```python
RECENT_MESSAGE_LIMIT = 12
SUMMARY_TRIGGER_MESSAGE_LIMIT = 40
SUMMARY_TRIGGER_CHAR_LIMIT = 24000
```

也就是说，只有当消息数量或内容长度增长到一定程度时，才会触发 summary 压缩。这样可以减少频繁压缩带来的额外 LLM 调用。

如果后续需要主动压缩，可以调用：

```python
context_manager.update_after_turn(
    context_state,
    final_messages,
    force_summarize=True,
)
```

### 18. 记忆系统详细设计

第 17 节已经说明了 PostgreSQL 记忆系统的整体作用。本节进一步说明数据库设计、长短期记忆实现方式、记忆来源和触发策略。

#### 18.1 记忆系统的分层

当前项目把记忆分成三层：

```text
完整历史归档：agent_messages
短期记忆：agent_sessions
长期记忆：agent_memories
```

三者职责不同：

```text
agent_messages
    保存完整历史。每轮用户输入、AI 回复、工具调用、工具结果都要落盘。

agent_sessions
    保存当前会话的压缩上下文状态。用于程序重启后继续对话。

agent_memories
    保存跨会话复用的信息。只保存稳定事实、偏好、决策和任务状态。
```

核心原则：

```text
数据库可以保存完整历史
模型上下文只能加载少量相关信息
```

#### 18.2 数据库表：agent_messages

`agent_messages` 是完整对话归档表。

表结构：

```sql
CREATE TABLE IF NOT EXISTS agent_messages (
    id BIGSERIAL PRIMARY KEY,
    session_id TEXT NOT NULL,
    role TEXT NOT NULL,
    content TEXT NOT NULL DEFAULT '',
    message_type TEXT NOT NULL,
    raw JSONB NOT NULL DEFAULT '{}',
    turn_index INTEGER NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

字段说明：

```text
id
    数据库自增主键。

session_id
    会话 ID。当前 CLI 默认使用 DEFAULT_SESSION_ID = "default"。

role
    消息角色，例如 user、assistant、tool、system。

content
    消息文本内容。用于展示、搜索和简单调试。

message_type
    LangChain 消息类型，例如 HumanMessage、AIMessage、ToolMessage。

raw
    LangChain message 的完整 JSON 结构。
    这样即使 content 不足以还原消息，也可以从 raw 里恢复更多细节。

turn_index
    第几轮用户对话。

created_at
    写入时间。
```

这张表每轮都会写。它用于：

```text
1. 展示完整历史会话
2. 审计 agent 做过什么
3. debug 工具调用和工具结果
4. 将来重新分析历史并提取记忆
```

注意：`agent_messages` 不直接作为模型输入。否则历史越长，上下文越容易失控。

#### 18.3 数据库表：agent_sessions

`agent_sessions` 是短期记忆表。

表结构：

```sql
CREATE TABLE IF NOT EXISTS agent_sessions (
    session_id TEXT PRIMARY KEY,
    summary TEXT NOT NULL DEFAULT '',
    recent_messages JSONB NOT NULL DEFAULT '[]',
    turn_index INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

字段说明：

```text
session_id
    会话 ID。

summary
    旧消息压缩后的摘要。

recent_messages
    最近若干条原始 LangChain 消息。

turn_index
    当前会话执行到第几轮。

created_at / updated_at
    创建和更新时间。
```

短期记忆的结构是：

```text
旧消息：进入 summary
近期消息：保留在 recent_messages
```

这两部分必须配套使用。不能只保存 summary，也不能只保存 recent messages。

#### 18.4 数据库表：agent_memories

`agent_memories` 是长期记忆表。

表结构：

```sql
CREATE TABLE IF NOT EXISTS agent_memories (
    id UUID PRIMARY KEY,
    scope TEXT NOT NULL,
    kind TEXT NOT NULL,
    content TEXT NOT NULL,
    tags JSONB NOT NULL DEFAULT '[]',
    importance INTEGER NOT NULL DEFAULT 3,
    confidence DOUBLE PRECISION NOT NULL DEFAULT 1.0,
    source_message_ids JSONB NOT NULL DEFAULT '[]',
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    archived_at TIMESTAMPTZ
);
```

字段说明：

```text
id
    长期记忆 ID。

scope
    作用域。当前主要使用 project，也支持 global。

kind
    记忆类型。

content
    记忆正文。

tags
    标签，用于分类和检索。

importance
    重要性。数值越高，越应该优先保留和检索。

confidence
    置信度。表示这条记忆是否可靠。

source_message_ids
    来源消息 ID。用于追溯记忆来自哪轮对话。

archived_at
    归档时间。非空表示这条记忆不再参与检索。
```

长期记忆的类型包括：

```text
user_preference
    用户偏好，例如“用户希望代码解释使用中文”。

project_fact
    项目事实，例如“当前项目使用 LangGraph + LangChain”。

decision
    架构决策，例如“子 agent 不允许继续创建子 agent”。

task_state
    长期任务状态，例如“正在实现 PostgreSQL 记忆系统”。

skill_note
    可复用经验，例如“读取大文件应优先委派给子 agent”。
```

不应该保存为长期记忆的内容：

```text
API Key
密码
token
.env 内容
大段工具输出
临时推理过程
一次性闲聊
明显过期的错误判断
```

#### 18.5 短期记忆如何实现

短期记忆由 `agent_context.py` 管理，数据结构是：

```python
@dataclass
class AgentContextState:
    summary: str = ""
    recent_messages: list = field(default_factory=list)
```

程序启动时：

```python
context_state, turn_index = memory_store.load_session(DEFAULT_SESSION_ID)
```

这一步从 `agent_sessions` 读取：

```text
summary
recent_messages
turn_index
```

每轮用户输入时，`build_input_messages()` 会构造模型输入：

```text
Conversation context summary
Relevant long-term memory
recent_messages
current user input
```

其中 `Conversation context summary` 来自 `agent_sessions.summary`。

`Relevant long-term memory` 来自 `agent_memories` 的检索结果。

`recent_messages` 来自 `agent_sessions.recent_messages`。

每轮结束后，`update_after_turn()` 会更新短期上下文：

```python
context_state = context_manager.update_after_turn(
    context_state,
    final_messages,
)
```

它会先过滤掉系统注入消息：

```text
Conversation context summary
Relevant long-term memory
```

这样做是为了避免把系统摘要和长期记忆再次当成普通聊天记录保存。

然后它判断是否需要压缩：

```python
RECENT_MESSAGE_LIMIT = 12
SUMMARY_TRIGGER_MESSAGE_LIMIT = 40
SUMMARY_TRIGGER_CHAR_LIMIT = 24000
SESSION_SUMMARY_MAX_CHARS = 4000
SUMMARY_SOURCE_CHAR_LIMIT = 12000
```

含义：

```text
RECENT_MESSAGE_LIMIT
    最近保留多少条原始消息。

SUMMARY_TRIGGER_MESSAGE_LIMIT
    消息数量超过多少后触发压缩。

SUMMARY_TRIGGER_CHAR_LIMIT
    消息总字符数超过多少后触发压缩。

SESSION_SUMMARY_MAX_CHARS
    summary 最大字符数。

SUMMARY_SOURCE_CHAR_LIMIT
    生成 summary 时最多喂给摘要模型多少字符。
```

如果没有达到阈值：

```text
不压缩，继续把消息放在 recent_messages 中。
```

如果达到阈值：

```text
旧消息 -> 调用非流式 LLM 压缩进 summary
最近 12 条消息 -> 原文保留
```

最后写回数据库：

```python
memory_store.save_session(DEFAULT_SESSION_ID, context_state, turn_index)
```

所以短期记忆具有两个能力：

```text
1. 控制模型输入长度
2. 支持程序重启后恢复会话上下文
```

#### 18.6 长期记忆如何实现

长期记忆由 `PostgresMemoryStore` 管理。

每轮开始前，主循环用当前用户输入检索长期记忆：

```python
memories = memory_store.retrieve_memories(user_input)
memory_message = memory_store.build_memory_message(memories)
```

检索 SQL 使用 PostgreSQL 全文检索和模糊匹配：

```sql
to_tsvector('simple', content) @@ plainto_tsquery('simple', query)
OR content ILIKE '%query%'
```

排序策略：

```text
全文检索相关度 DESC
importance DESC
updated_at DESC
```

检索结果会变成一个 `SystemMessage`：

```text
Relevant long-term memory:
- [project_fact | project | importance=5 | tags=postgres] 当前项目使用 PostgreSQL 保存记忆。
```

这个消息只参与本轮推理，不会再次保存进短期上下文。

#### 18.6.1 SQL 集中管理和注入防护

当前项目已经把数据库 SQL 做了集中管理。

文件结构：

```text
agent_sql.py
    统一保存业务查询 SQL 常量。
    例如 SELECT_SESSION_CONTEXT、UPSERT_SESSION_CONTEXT、INSERT_AGENT_EVENT。

sql/schema.sql
    统一保存建表和建索引 SQL。
    PostgresMemoryStore.initialize() 会读取并执行这个文件。

tests/test_sql.py
    测试专用 SQL 常量，避免测试文件里嵌入大段 SQL。
```

这样做的目的：

```text
1. schema 不散落在 Python 方法里
2. 业务查询有统一命名，方便搜索和维护
3. 测试 SQL 和生产 SQL 分开管理
4. 后续如果引入迁移工具，可以从 sql/schema.sql 平滑演进
```

防 SQL 注入的核心规则是：

```text
SQL 结构可以写成常量。
运行时数据必须通过 psycopg 参数绑定传入。
不要用 f-string、format、字符串拼接把用户输入拼进 SQL。
```

正确写法：

```python
cur.execute(
    SELECT_SESSION_CONTEXT,
    (session_id,),
)
```

错误写法：

```python
cur.execute(f"SELECT * FROM agent_sessions WHERE session_id = '{session_id}'")
```

当前代码中用户输入相关的数据，例如：

```text
session_id
query
scope
memory content
tool/event payload
```

都通过 `%s` 占位符传给 psycopg：

```python
cur.execute(
    SELECT_RELEVANT_MEMORIES,
    (
        scope,
        normalized_query,
        f"%{query[:120]}%",
        normalized_query,
        self.retrieval_limit,
    ),
)
```

注意这里的 `f"%{query[:120]}%"` 只是构造一个参数值，不是拼接 SQL 语句，所以仍然是安全的。

另外，PostgreSQL 连接参数不再手写拼接成连接字符串，而是使用：

```python
make_conninfo(
    "",
    host=self.host,
    port=self.port,
    dbname=self.dbname,
    user=self.user,
    password=self.password,
)
```

这样可以避免用户名或密码中包含空格、特殊字符时破坏连接串。

#### 18.7 记忆是怎么来的

长期记忆主要有两种来源。

第一种是人工或测试直接写入数据库。

例如 `tests/test_memory_store.py` 会手动插入一条 `agent_memories`，用于验证读写链路。

第二种是从对话中自动提取。

每轮结束后，程序已经把本轮消息写入 `agent_messages`。如果满足触发条件，就调用：

```python
extract_and_save_memories(
    session_id,
    turn_index,
    turn_messages,
    source_message_ids,
)
```

这个函数会把本轮消息格式化后交给非流式 LLM，让模型返回严格 JSON：

```json
[
  {
    "kind": "user_preference",
    "content": "用户希望代码解释使用中文。",
    "tags": ["language", "style"],
    "importance": 4,
    "confidence": 0.9
  }
]
```

然后程序进行过滤：

```text
importance < MEMORY_MIN_IMPORTANCE
    不保存。

content 为空
    不保存。

疑似包含 api_key / password / token / .env
    不保存。

同 scope、同 kind、内容完全相同或前 160 字符相同
    更新已有记忆。

其他情况
    新增记忆。
```

保存时会记录：

```text
source_message_ids
```

这样后续可以知道这条长期记忆来自哪些完整历史消息。

#### 18.8 长期记忆提取策略

长期记忆不应该每轮都提取。原因是：

```text
1. 每次提取都需要额外 LLM 调用。
2. 很多轮普通对话没有长期价值。
3. 频繁提取会增加成本，也可能污染长期记忆。
```

当前配置：

```python
MEMORY_EXTRACTION_ENABLED = True
MEMORY_EXTRACTION_ASYNC = True
MEMORY_EXTRACTION_INTERVAL_TURNS = 5
MEMORY_EXTRACTION_MIN_CHARS = 1200
MEMORY_EXTRACTION_HINT_KEYWORDS = [
    "记住",
    "保存",
    "偏好",
    "约定",
    "规则",
    "以后",
    "长期",
    "remember",
    "preference",
    "always",
]
```

触发条件由 `agent_loop.py` 中的 `_should_extract_long_term_memory()` 控制。

满足任意一个条件才会提取：

```text
1. 用户输入包含记忆提示词
2. 当前轮次 turn_index 能被 5 整除
3. 本轮消息总字符数 >= 1200
```

这三个条件分别覆盖：

```text
显式记忆
    用户明确说“记住”“以后都这样”等。

周期沉淀
    每隔几轮检查一次，避免漏掉没有显式说“记住”的重要信息。

大内容沉淀
    长工具结果或复杂讨论更可能包含可复用信息。
```

长期记忆提取默认异步执行：

```python
MEMORY_EXTRACTION_ASYNC = True
```

也就是说，AI 回复完成后，程序会尽快回到下一次 `You:`。长期记忆提取在后台线程继续执行。

有一个例外：如果用户输入里包含“记住 / 偏好 / 约定 / 规则 / 以后”等显式记忆提示词，本轮会同步提取长期记忆。

这样做是为了保证用户明确要求“记住”时，程序不会因为后台任务还没执行完而让用户马上查库却看不到记录。

如果显式记忆请求已经处理，但最终没有保存任何长期记忆，CLI 会提示：

```text
Memory note: explicit memory request was processed, but no long-term memory was saved.
```

这通常说明模型返回了空结果、候选记忆重要性太低，或者内容被敏感信息过滤规则拦截。

#### 18.9 每轮执行流程

程序启动时：

```text
1. 初始化 PostgresMemoryStore
2. 创建表和索引
3. 从 agent_sessions 加载短期上下文
```

每轮用户输入时：

```text
1. 空输入直接忽略
2. 从 agent_memories 检索相关长期记忆
3. 构造模型输入
4. 执行 LangGraph agent/tool 循环
5. 将本轮消息写入 agent_messages
6. 更新 AgentContextState
7. 保存 agent_sessions
8. 如果满足策略条件，后台提取 agent_memories
```

三张表的写入频率不同：

```text
agent_messages
    每轮都写。

agent_sessions
    每轮都更新。

agent_memories
    只有满足长期记忆提取策略时才写。
```

这个区别很重要：

```text
完整历史必须可靠保存
短期上下文必须可恢复
长期记忆必须克制写入
```

#### 18.10 打开历史会话时如何处理

如果后续做 UI，打开一个历史会话时应该分成两条路径。

界面展示完整历史：

```sql
SELECT *
FROM agent_messages
WHERE session_id = ?
ORDER BY turn_index, id;
```

模型继续对话时加载短期上下文：

```sql
SELECT summary, recent_messages, turn_index
FROM agent_sessions
WHERE session_id = ?;
```

也就是说：

```text
用户界面看到完整历史
模型上下文只看到 summary + recent_messages + relevant long-term memory
```

不要为了恢复上下文而把 `agent_messages` 的完整历史全部重新塞给模型。

#### 18.11 当前测试覆盖

测试文件在 `tests/` 目录。

`tests/test_memory_store.py` 验证 PostgreSQL 读写链路：

```text
1. 写入 agent_sessions
2. 读取 agent_sessions
3. 写入 agent_messages
4. 查询 agent_messages
5. 写入 agent_memories
6. 检索 agent_memories
7. 删除测试数据
```

`tests/test_memory_extraction_policy.py` 验证长期记忆提取策略：

```text
1. 显式“记住”类输入会触发
2. 每隔 5 轮会触发
3. 本轮内容超过 1200 字符会触发
4. 普通短对话不会触发
```

### 19. 统一 Agent Hook / Event 系统

当前项目已经加入统一事件系统，用来观察 Agent 每一轮执行过程。

它不是 Git hook 那种外部脚本，也不是用来修改业务流程的拦截器。当前 v1 的定位是：

```text
只记录事实
不改变流程
不影响回答结果
```

#### 19.1 为什么需要统一事件系统

Agent 一轮回答里可能发生很多事情：

```text
加载上下文
检索长期记忆
调用 LLM
发起工具调用
收到工具结果
压缩上下文
提取长期记忆
保存数据库
处理异常
```

如果只靠 `print` 或零散 debug，很难回答这些问题：

```text
这一轮为什么慢？
调用了哪些工具？
哪个工具失败了？
长期记忆为什么没有保存？
上下文有没有压缩？
模型调用是否被 API 拦截？
某个 run_id 的完整链路是什么？
```

所以现在使用统一的 `agent_events` 表记录结构化事件。

#### 19.2 核心概念

事件系统分成三层：

```text
Hook
    代码中的触发点，例如 turn_started、tool_finished。

Event
    hook 触发后生成的结构化记录。

Sink
    event 的输出位置，例如 PostgreSQL、控制台、JSONL 文件。
```

对应代码在 `agent_hooks.py`。

核心数据结构：

```python
@dataclass
class AgentEvent:
    event_type: str
    source: str
    message: str = ""
    payload: dict = field(default_factory=dict)
    level: str = "info"
    session_id: str = DEFAULT_SESSION_ID
    turn_index: int | None = None
    run_id: str = ""
    duration_ms: int | None = None
    created_at: datetime = field(default_factory=lambda: datetime.now(timezone.utc))
```

字段含义：

```text
event_type
    事件类型，例如 turn_started、tool_finished。

source
    事件来源模块，例如 agent_loop、agent_stream、agent_memory。

message
    简短说明。

payload
    结构化附加信息，只放摘要，不放完整大内容。

level
    info / warning / error。

session_id
    会话 ID。

turn_index
    当前第几轮对话。

run_id
    单轮运行 ID。同一轮所有事件共享同一个 run_id。

duration_ms
    可选耗时字段，后续可以用于性能统计。

created_at
    事件创建时间。
```

#### 19.3 事件上下文

每轮用户输入后，`agent_loop.py` 会生成一个 `run_id`：

```python
run_id = str(uuid4())
set_event_context(DEFAULT_SESSION_ID, current_turn_index, run_id)
```

后续本轮中调用：

```python
emit_event(...)
```

都会自动带上：

```text
session_id
turn_index
run_id
```

这样就可以用 `run_id` 查询一整轮发生过的所有事件。

#### 19.4 数据库表：agent_events

事件主存储是 PostgreSQL 表 `agent_events`。

表结构：

```sql
CREATE TABLE IF NOT EXISTS agent_events (
    id BIGSERIAL PRIMARY KEY,
    run_id TEXT NOT NULL,
    session_id TEXT NOT NULL,
    turn_index INTEGER,
    event_type TEXT NOT NULL,
    source TEXT NOT NULL,
    level TEXT NOT NULL DEFAULT 'info',
    message TEXT NOT NULL DEFAULT '',
    payload JSONB NOT NULL DEFAULT '{}',
    duration_ms INTEGER,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

索引：

```sql
CREATE INDEX IF NOT EXISTS idx_agent_events_session_turn
ON agent_events(session_id, turn_index, id);

CREATE INDEX IF NOT EXISTS idx_agent_events_run
ON agent_events(run_id);

CREATE INDEX IF NOT EXISTS idx_agent_events_type
ON agent_events(event_type);
```

常用查询：

```sql
-- 查看某一轮完整事件链路
SELECT event_type, source, level, message, payload, created_at
FROM agent_events
WHERE run_id = '...'
ORDER BY id;

-- 查看某个 session 的最近事件
SELECT event_type, source, message, created_at
FROM agent_events
WHERE session_id = 'default'
ORDER BY id DESC
LIMIT 50;

-- 查看最近失败事件
SELECT *
FROM agent_events
WHERE level = 'error'
ORDER BY id DESC
LIMIT 20;
```

#### 19.5 Sink 设计

当前内置四种 sink：

```text
PostgresEventSink
    写入 agent_events 表。默认启用。

ConsoleEventSink
    输出到控制台。默认关闭。

JsonlFileEventSink
    写入 logs/agent_events.jsonl。默认关闭。

NoopEventSink
    丢弃事件。
```

配置在 `agent_config.py`：

```python
AGENT_EVENTS_ENABLED = True
AGENT_EVENTS_POSTGRES_ENABLED = True
AGENT_EVENTS_CONSOLE_ENABLED = False
AGENT_EVENTS_FILE_ENABLED = False
AGENT_EVENTS_FILE_PATH = "logs/agent_events.jsonl"
AGENT_EVENTS_PAYLOAD_PREVIEW_LIMIT = 1000
AGENT_EVENTS_ASYNC_WRITE = True
AGENT_EVENTS_BATCH_SIZE = 50
AGENT_EVENTS_FLUSH_INTERVAL_SECONDS = 1.0
AGENT_EVENTS_QUEUE_MAX_SIZE = 1000
```

事件 sink 有一个重要约束：

```text
sink 写入失败不能影响 Agent 主流程
```

也就是说，如果 PostgreSQL 写事件失败，Agent 不应该因此无法回答。

#### 19.5.1 PostgreSQL 异步批量写入

事件系统默认不会在 `emit_event()` 中同步写 PostgreSQL。

当前流程是：

```text
emit_event()
    -> 事件进入内存队列
    -> 立即返回，不阻塞 Agent 主流程

后台 writer 线程
    -> 按批量或定时 flush
    -> 批量 INSERT 到 agent_events
```

对应配置：

```python
AGENT_EVENTS_ASYNC_WRITE = True
AGENT_EVENTS_BATCH_SIZE = 50
AGENT_EVENTS_FLUSH_INTERVAL_SECONDS = 1.0
AGENT_EVENTS_QUEUE_MAX_SIZE = 1000
```

含义：

```text
AGENT_EVENTS_ASYNC_WRITE
    是否启用异步写入。

AGENT_EVENTS_BATCH_SIZE
    最多积累多少条事件后批量写入。

AGENT_EVENTS_FLUSH_INTERVAL_SECONDS
    最长等待多久就 flush 一次。

AGENT_EVENTS_QUEUE_MAX_SIZE
    内存队列最大长度。队列满时会丢弃当前事件并输出 debug。
```

程序退出时，`PostgresEventSink.close()` 会尝试 flush 队列中剩余事件。

测试中如果需要确保事件已经落库，可以调用：

```python
sink.flush()
sink.close()
```

#### 19.5.2 可复用 hook 组件：三层 API

当前 hook API 分三层：

```text
基础层
    emit_event()
    只在没有专用 helper 时使用。

便捷层
    event_span()
    record_error()
    record_tool_started()
    record_tool_finished()
    record_tool_failed()
    record_command_started()
    record_command_finished()
    record_command_failed()
    record_memory_saved()

接入层
    ObservedToolNode
    负责把工具调用边界事件集中接入 ToolNode wrapper。
```

`event_span()` 是便捷层的一部分，适合记录一个有开始、结束、失败的耗时操作：

```python
from agent_hooks import event_span

with event_span(
    "load_skill",
    source="agent_skills",
    payload={"skill": skill_name},
):
    skill = store.read_skill(skill_name)
```

它会自动记录：

```text
load_skill_started
load_skill_finished
load_skill_failed
duration_ms
```

这样新功能接入 hook 时，只需要围住关键操作，不需要手写 started / finished / failed 三套事件。

便捷层还提供常用 helper，用来统一事件命名和 payload 结构：

```python
record_error(
    source="agent_loop",
    operation="llm",
    error=exc,
    event_type="llm_failed",
)

record_command_failed(
    source="agent_tools",
    reason="docker_unavailable",
    command="python -m unittest",
    detail="Docker Desktop is not running.",
)

record_memory_saved(
    source="agent_memory",
    memory_id="...",
    action="created",
    kind="project_fact",
    importance=5,
    content="...",
)
```

这些 helper 的作用：

```text
1. 避免各模块手写不同格式的 payload
2. 保持 event_type 命名一致
3. 后续修改事件结构时，只需要改 helper
4. 新功能接入时更快，不需要重新设计事件格式
```

helper 由 `agent_hooks.py` 中的 `HOOK_HELPERS` 统一注册，可以通过下面的方法查看：

```python
from agent_hooks import list_hook_helpers

helpers = list_hook_helpers()
```

注册表的作用是：

```text
1. 明确有哪些 helper 是公共 API
2. 明确每个 helper 负责哪些 event_type
3. 避免新功能随意新增一套命名风格
4. 方便后续生成文档或做自动检查
```

#### 19.6 安全和截断规则

事件 payload 不保存完整 prompt、完整工具输出、完整文件内容。

payload 会做两类处理：

```text
1. 敏感字段过滤
2. 长文本截断
```

敏感字段包括：

```text
api_key
apikey
password
passwd
token
secret
authorization
.env
```

如果 payload key 命中这些词，会被替换成：

```text
[REDACTED]
```

长文本会按照：

```python
AGENT_EVENTS_PAYLOAD_PREVIEW_LIMIT = 1000
```

进行截断。

#### 19.7 当前已经接入的事件

`agent_loop.py`：

```text
turn_started
context_loaded
llm_started
llm_finished
llm_failed
memory_extract_triggered
memory_extract_skipped
turn_finished
turn_failed
```

`agent_observed_tools.py`：

```text
tool_started
tool_finished
tool_failed
```

这些工具边界事件不是写在每个 tool 函数里，也不是从 SSE stream 里推导出来的，而是由 `ObservedToolNode` 统一记录。

`agent_stream.py`：

```text
recursion_limit
llm_or_graph_failed
```

`agent_context.py`：

```text
context_summary_skipped
context_summarize_triggered
context_summarized
context_summary_failed
```

`agent_memory.py`：

```text
context_loaded
context_saved
messages_archived
memory_retrieved
memory_extract_triggered
memory_extract_finished
memory_saved
memory_skipped
memory_failed
```

`agent_tools.py` 中的容器命令工具：

```text
command_started
command_finished
command_failed
```

#### 19.8 当前测试覆盖

`tests/test_agent_hooks.py` 验证：

```text
emit_event 能写入测试 sink
sink 失败不会中断主流程
payload 敏感字段会被过滤
payload 长文本会被截断
event context 会正确注入 session_id / turn_index / run_id
helper 注册表包含当前公共 helper
ObservedToolNode 会自动记录 tool_started / tool_finished
```

`tests/test_memory_store.py` 额外验证：

```text
PostgresEventSink 能写入 agent_events
可以按 run_id 查询事件
测试事件可以删除
```

#### 19.9 Hook 使用方法

新功能接入 hook 时，优先考虑三个问题：

```text
1. 这个功能是否值得观测？
2. 应该记录 started / finished / failed，还是只记录一个事实事件？
3. payload 里应该放哪些 metadata，而不是完整内容？
```

一般规则：

```text
耗时操作
    使用 event_span。

明确的业务事实
    使用 emit_event 或对应 record_* helper。

错误
    使用 record_error。

工具调用
    不在每个 tool 函数里手写 hook。
    统一通过 ObservedToolNode 自动记录工具边界事件。

长期记忆保存
    使用 record_memory_saved。
```

##### 19.9.1 直接记录一个事件

如果只是记录一个事实，可以直接使用 `emit_event()`：

```python
from agent_hooks import emit_event

emit_event(
    "skill_manifest_loaded",
    "agent_skills",
    "Loaded local skill manifest.",
    {
        "skill_count": len(skills),
    },
)
```

适用场景：

```text
加载了某个 manifest
检索到了多少条记忆
跳过了某个操作
保存了某个状态
```

注意：`payload` 放结构化摘要，不要放完整文件、完整 prompt、完整工具输出。

##### 19.9.2 记录一个耗时操作

如果一个功能有明显的开始、结束、失败过程，优先使用 `event_span()`：

```python
from agent_hooks import event_span

with event_span(
    "load_skill",
    source="agent_skills",
    payload={"skill_name": skill_name},
):
    skill = store.read_skill(skill_name)
```

它会自动产生：

```text
load_skill_started
load_skill_finished
load_skill_failed
```

并自动记录：

```text
duration_ms
```

如果代码块中抛异常，`event_span()` 会记录 failed 事件，然后继续抛出异常，不会吞掉业务错误。

适用场景：

```text
文件总结
skill 加载
子 agent 执行
外部 API 调用
数据库批处理
复杂工具执行
```

当前项目里已经接入的真实业务路径：

```text
agent_context.py
    context_summary_llm_started
    context_summary_llm_finished
    context_summary_llm_failed

agent_memory.py
    memory_candidate_extract_started
    memory_candidate_extract_finished
    memory_candidate_extract_failed
```

##### 19.9.3 记录错误

错误事件使用 `record_error()`：

```python
from agent_hooks import record_error

try:
    response = llm.invoke(messages)
except Exception as exc:
    record_error(
        source="agent_loop",
        operation="llm",
        error=exc,
        message="Parent LLM call failed.",
        event_type="llm_failed",
        payload={"message_count": len(messages)},
    )
    raise
```

它会统一 payload：

```text
operation
error_type
error
```

这样查询错误时，结构是稳定的。

##### 19.9.4 记录工具事件

工具边界事件集中在 `ToolNode` wrapper 层记录。

当前 `agent_loop.py` 使用：

```python
from agent_observed_tools import ObservedToolNode

builder.add_node("tools", ObservedToolNode(parent_tools))
```

`ObservedToolNode` 内部会在工具执行前后调用专用 helper：

```python
from agent_hooks import record_tool_failed, record_tool_finished, record_tool_started

record_tool_started(...)
record_tool_finished(...)
record_tool_failed(...)
```

它们会统一工具事件的字段：

```text
tool
tool_call_id
args_preview
content_preview
content_chars
error_type
error
duration_ms
```

这个设计有一个明确规则：

```text
普通工具函数只写业务逻辑。
通用工具边界事件放在 ObservedToolNode。
工具内部的子步骤事件才写在工具函数内部。
```

例如 `run_command_in_container` 是一个工具。它的通用边界事件：

```text
tool_started
tool_finished
tool_failed
```

由 `ObservedToolNode` 自动记录。

但它内部还会执行 Docker 命令，所以内部子步骤使用：

```python
record_command_started(...)
record_command_finished(...)
record_command_failed(...)
```

这些是命令执行事件，不是通用工具边界事件。

##### 19.9.5 记录长期记忆保存

长期记忆保存使用 `record_memory_saved()`：

```python
from agent_hooks import record_memory_saved

record_memory_saved(
    source="agent_memory",
    memory_id=memory_id,
    action="created",
    kind="project_fact",
    importance=5,
    content=content,
)
```

它会生成统一 payload：

```text
memory_id
action
kind
importance
content_preview
```

这样后续查询 `memory_saved` 时，不会因为不同模块手写 payload 而字段不一致。

##### 19.9.6 新功能接入 checklist

给一个新功能加 hook 时，可以按这个 checklist 做：

```text
1. 是否是耗时操作？
   是：优先使用 event_span。

2. 是否是错误路径？
   是：使用 record_error。

3. 是否已有专用 helper？
   例如 record_tool_started / record_tool_finished / record_tool_failed / record_memory_saved。

4. payload 是否包含敏感信息？
   如果有 api_key / password / token / .env，不要放入 payload。

5. payload 是否过大？
   只放 preview、数量、ID、状态，不放完整内容。

6. 是否需要查询？
   如果未来要按某字段查，字段应放在 payload 的固定 key 中。
```

示例：给一个新的网页搜索工具加 hook：

```python
from agent_hooks import event_span

def web_search(query: str) -> str:
    with event_span(
        "web_search",
        source="agent_tools",
        payload={"query_preview": query[:200]},
    ):
        return call_search_api(query)
```

这样自动记录：

```text
web_search_started
web_search_finished
web_search_failed
```

#### 19.10 未来数据库读写优化方向

当前事件写入已经从“每个事件同步写 PostgreSQL”优化为：

```text
内存队列 + 后台线程 + 批量 INSERT
```

这已经足够支撑当前本地单进程 CLI Agent。

但如果后续事件量变大，还可以继续优化。

##### 19.10.1 连接复用或连接池

当前后台 writer 每次批量写入时会创建 PostgreSQL 连接。

未来可以改成：

```text
后台 writer 持有长期连接
或者使用 psycopg connection pool
```

优点：

```text
减少频繁建连开销
降低批量写入延迟
```

需要注意：

```text
连接断开后要自动重连
程序退出时要正确关闭连接
```

##### 19.10.2 更强的批量策略

当前策略是：

```python
AGENT_EVENTS_BATCH_SIZE = 50
AGENT_EVENTS_FLUSH_INTERVAL_SECONDS = 1.0
```

后续可以按事件等级区分：

```text
error / warning
    更快 flush，避免关键错误延迟过久。

info
    可以更大批量写入。
```

也可以针对不同 event_type 做采样：

```text
高频 info 事件
    采样或降低写入频率。

失败事件
    永远写入。
```

##### 19.10.3 队列满时的降级策略

当前队列满时，会丢弃当前事件并输出 debug。

未来可以更细：

```text
error 事件
    尽量保留。

info 事件
    可以丢弃。

tool_failed / memory_failed
    优先级更高。
```

可选做法：

```text
1. 使用优先级队列
2. 队列满时丢弃最旧的 info 事件
3. 单独保留 error 队列
```

##### 19.10.4 Redis Stream 作为跨进程队列

当前内存队列是进程内队列，适合本地单进程。

如果未来变成多进程或 Web 服务，可以考虑：

```text
emit_event()
    -> Redis Stream

独立 event worker
    -> XREADGROUP 批量读取
    -> 写入 PostgreSQL
```

Redis 适合解决：

```text
多个 Agent 进程同时写事件
Agent 进程和事件写入 worker 解耦
PostgreSQL 短暂不可用时先缓冲
进程崩溃时减少事件丢失
```

但当前阶段不需要马上引入 Redis。当前单进程 CLI 场景下：

```text
Python queue.Queue + 后台线程
```

更简单，也更容易维护。

##### 19.10.5 agent_events 表清理和归档

事件表会持续增长。未来需要设计保留策略：

```text
保留最近 N 天事件
保留 error/warning 更久
定期归档旧事件
删除低价值 info 事件
```

例如：

```sql
DELETE FROM agent_events
WHERE level = 'info'
  AND created_at < now() - interval '30 days';
```

也可以做按月归档表：

```text
agent_events_2026_06
agent_events_2026_07
```

##### 19.10.6 事件查询 API

未来如果做 UI 或调试命令，可以提供查询接口：

```text
list_recent_events(session_id, limit)
list_turn_events(run_id)
list_failed_events(limit)
list_tool_events(session_id)
```

这样不用直接写 SQL，也方便在 CLI/Web UI 中展示。
